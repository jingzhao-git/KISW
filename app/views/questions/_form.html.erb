<div class="container">
  <% if @question.errors.any? %>
    <h2>The following errors prevented the question from being saved</h2>
    <ul>
    <% @question.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
    <% end %>
    </ul>
  <% end %>
    <div class="row justify-content-center">
    <div class="col-10">
      <%= form_with(model: @question, class: "shadow p-5 mb-5 bg-info rounded", local: true) do |f| %>
        <div class="form-group row">
          <%= f.label :title, class: "col-2 col-form-label text-light" %>
       <div class="col-10"> 
            <%= f.text_area :title, rows:2, class: "form-control shadow rounded", placeholder: "Please summarize your question in one sentence🤓" %>
          </div>
        </div>
                <div class="form-group row">
          <%= f.label :description, class: "col-2 col-form-label text-light" %>
          <div class="col-10"> 
            <%= f.text_area :text, rows: 10, class: "form-control shadow rounded", placeholder: "Please tell us more details about your question🧐" %>
          </div>
        </div>
                <div class="form-group row justify-content-center">
          <%= f.submit class: "btn btn-outline-light btn-lg" %>
        </div>
>
      <% end %>

</div> 